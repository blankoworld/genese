#!/usr/bin/env bash
#
# .env
#
# GENESE project configuration parameters available on Github:
# github.com/blankoworld/genese
#
# Non-exhaustive list of available variable and their use.
#
# - PROFIL: Profil name used by default for `genese -p` command
# - INSTANCES_DIR: Directory in which instances would be
# - CONFIGS_DIR: Where to find different configs.
# - SERVICES_DIR: Directory where different used services (in configs) are described.
# - SOURCE: Git repository in which to find all needed files
# - DOCKER_SOCKET: Docker socket absolute address

CONFIGS_DIR="${BASE_DIR}/configs"
SERVICES_DIR="${BASE_DIR}/services"
DOCKER_SOCKET="/var/run/docker.sock"

# vim: ts=2 sw=2 et nu syn=bash